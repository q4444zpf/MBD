# 水利专用 MBD 软件产品需求文档 V1.0

| 文档属性 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 文档状态 | 立项评审稿 |
| 编制日期 | 2026-09-18 |
| 适用对象 | 产品、领域专家、架构、研发、测试、实施、运维、项目管理与验收人员 |
| 需求优先级 | P0：MVP 必需；P1：首期增强；P2：中长期扩展 |
| 技术栈 | TBD，须在架构评审后确定 |

> 本文档用于立项评审、范围确认、架构设计、研发拆解和验收。文中容量、时延、可用性及 AI 质量阈值为建议基线，须使用真实工程样本验证后冻结。

## 1. 产品概述

### 1.1 MBD 定义

本产品中的 MBD 指 Model-Based Design（基于模型的设计）。其核心是先建立可计算、可验证的工程模型，再以模型贯穿对象设计、设备与传感器布置、算法构建、工况仿真、调度控制和设计验证全过程。

**边界说明：** 机械制造领域的 MBD 有时表示 Model-Based Definition（基于模型的定义）。本产品明确采用 Model-Based Design 的含义。

### 1.2 背景与问题

- 水利工程涉及多类设计规范、设备标准、监测要求和数据标准，规则复杂且容易漏填、错填。
- 构筑物、设备、传感器、算法和工况通常分散在不同工具或文档中，缺少统一对象模型。
- 传统固定表单难以覆盖不同工程类型及自定义设备，扩展成本高。
- 设计、仿真、调度和控制相互割裂，模型间算法调用与联合验证困难。
- 评审环节缺少可追溯的自动校验结果，难以快速回答“工程在什么工况下可以运行、属于什么等级”。

### 1.3 产品定位

面向水利工程设计与建模人员的 MBD 设计与验证平台。产品以水利工程对象模型为中心，提供可配置对象库、拖拽式设计工作台、自然语言设计助手、算法与工况编排、联合仿真、标准校验和验证报告能力。

### 1.4 当前水利设计院常用做法与机会

当前水利设计院的信息化成熟度因单位、专业和项目类型而异，本节描述的是需要在立项调研中验证的行业共性，不代表所有设计院均采用完全相同的流程。

| 现状 | 常用做法 | 主要问题 | 产品机会 |
| --- | --- | --- | --- |
| 二维设计仍是主流 | 以 CAD 图纸、二维流程图、断面图和表格表达方案 | 图形与计算参数分离，设计变更难同步到计算和校核 | 将二维拓扑与结构化对象、参数、规则和运行结果建立绑定 |
| 文件驱动为主 | 通过 Word、Excel、PDF、图纸、计算书和共享目录传递输入与成果 | 版本命名依赖人工，数据来源和修改链难追溯 | 用项目、模型版本、场景、运行快照和成果链统一管理 |
| 专业软件分散 | 水文、水力、水质、调度、控制和制图分别使用不同软件 | 输入重复录入，接口格式不统一，联合验证成本高 | 建立标准对象模型与求解器适配层，编排跨模型计算 |
| 规范检索依赖经验 | 工程师从规范、企业标准、历史计算书中人工查找条款 | 新旧版本混用、适用条件判断困难、遗漏风险高 | 建设有版本、效力、权限和原文定位的知识库与规则库 |
| BIM/GIS 局部应用 | BIM 多用于重点构筑物或展示，GIS 多用于底图、空间分析和资产定位 | 尚未普遍贯穿模型计算、设计校核和成果审批全流程 | 先提供开放接口和关联能力，再按价值扩展二维/三维协同 |
| 评审依赖离线流转 | 通过邮件、会议纪要、批注文件完成复核和审批 | 意见、修改、计算输入和最终成果之间关系不清晰 | 用版本化工作流、问题闭环和不可变成果形成审计链 |

因此，产品机会不在于简单替换现有绘图或专业计算软件，而在于建立跨工具的统一水利对象语义、可复现运行上下文和可审计工程流程。

### 1.5 建议方案

建设一套水利专用 MBD 平台，以可配置组件为基础，在可视化画布中构建工程拓扑；以工程模型、方案场景和运行快照管理仿真、控制与调度计算；以知识库和检索增强生成支持自然语言建模、设计文件与校核报告草案；以工作流和成果管理固化复核、审批、发布和归档过程。

工程数值计算、拓扑合法性、单位、参数范围及强制性规则由确定性规则引擎或专业求解器执行。AI 仅负责意图理解、授权知识检索、草案生成和结果解释；AI 输出不得直接成为正式模型或成果，必须包含可定位引用，并经过确定性校验与人工审批。

## 2. 产品目标与成功指标

### 2.1 产品目标

1. 让设计人员通过拖拽和参数配置完成水利对象拓扑及可运行模型的构建。
2. 建立可扩展、可版本化、可治理的水利组件与对象体系。
3. 打通工程模型、算法、工况、仿真、调度、校核与成果的上下文。
4. 将规范、设计规则、校核文件和历史成果转化为可检索、可引用的知识资产。
5. 固化设计、校核、审批和归档流程，使模型和成果可复现、可追溯、可审计。

### 2.2 成功指标

上线前须采集至少 2 至 4 周现状数据，并由产品、领域、技术和验收方共同冻结统计口径。

| 目标 | 指标口径 | V1.0 建议目标 |
| --- | --- | --- |
| 建模效率 | 标准案例从需求输入到可运行模型的中位耗时 | 较现状下降不少于 40% |
| 上手效率 | 新用户完成 2 小时培训后独立搭建标准拓扑的比例 | 不少于 80% |
| 组件复用 | 新模型中引用已发布组件模板的节点占比 | 不少于 80% |
| 自动校核价值 | 自动校核发现的问题占全部发现问题的比例 | 不少于 70% |
| 文档解析 | 受支持文档解析成功率 | 不少于 98% |
| 原文定位 | 已生成切片可回到页码、章节或表格的比例 | 100% |
| 检索质量 | 领域评测集 Recall@10 与 nDCG@10 | 不少于 90% 与 0.80 |
| AI 引用 | 关键结论具有有效引用的比例 | 100% |
| AI 草案质量 | 草案首次通过或仅需轻微修改的比例 | 不少于 70% |
| 校核安全 | 高风险规则集严重问题召回率 | 不少于 95% |
| 运行稳定 | 排除输入错误后的模型任务成功率 | 不少于 98% |
| 联合仿真复现 | 同一快照重复运行的黄金算例结果 | 关键指标全部处于领域专家冻结的容差内 |
| 成果追溯 | 抽检成果具备完整版本与审批链的比例 | 100% |
| 系统可用性 | MVP 月度可用性 | 不少于 99.5% |

### 2.3 产品原则

- **模型为中心：** 图形、参数、算法、数据、规则、运行和成果均关联到统一模型对象。
- **发布即不可变：** 已发布组件、模型、规则、知识、工作流和成果不得原地覆盖。
- **计算可复现：** 正式运行必须冻结全部输入、依赖及环境版本。
- **确定性优先：** 工程数值和强制性判断不能只由语言模型给出。
- **人机协同：** AI 只生成草案，引用、校验、人工复核和审批缺一不可。
- **权限同源：** 页面、API、搜索、向量检索、文件下载和 AI 上下文使用同一权限结论。
- **开放集成：** 通过版本化适配器接入专业求解器、数据源、BIM、GIS 和档案系统。

## 3. 用户角色与职责边界

### 3.1 用户角色

| 角色 | 主要任务 | 核心诉求 |
| --- | --- | --- |
| 系统管理员 | 组织、用户、角色、权限、菜单、参数、日志与服务治理 | 系统安全可控，关键操作有审计 |
| 组件管理员与领域专家 | 组件模板、参数 Schema、端口、单位、规则、版本 | 标准组件可复用，升级不破坏历史模型 |
| 知识管理员 | 文档解析、切片、元数据、审核、发布、废止 | AI 仅使用有效且获授权的知识 |
| 模型、算法与工作流管理员 | 模型类型、算法资产、求解器适配器、流程模板 | 项目采用统一模型、算法和流程规范 |
| 项目经理 | 项目成员、里程碑、流程发起、成果提交 | 掌握进度、阻塞和审批状态 |
| 设计与建模工程师 | 拓扑、参数、场景、运行、AI 草案 | 快速完成可运行、可解释的设计 |
| 校核与审核人员 | 规则执行、问题复核、退回、批准 | 结论有依据，修改可追踪 |
| 查看与审计人员 | 查询、追溯、授权下载、日志导出 | 不修改数据即可核查全过程 |

### 3.2 管理端与用户端边界

| 入口 | 能力范围 | 责任边界 |
| --- | --- | --- |
| 管理端 | 模型类型、工作流模板、智能体、知识、组件、成果策略、用户、角色、权限、菜单、日志 | 负责组织级标准、资产治理和安全审计，不默认参与项目工程设计 |
| 用户端 | 项目、拖拽建模、参数配置、校核、AI 对话、模型运行、测试报告、评估报告、成果 | 负责项目内生产、复核和交付，不能发布全局组件或平台规则 |
| 系统服务 | 鉴权、运行调度、规则执行、审计、存储、索引和健康监测 | 默认拒绝；任何未授权对象在页面、搜索和 API 中均不可见 |

### 3.3 职责分离规则

- 组件管理员可发布全局模板，但不能静默覆盖项目中的组件实例。
- 知识管理员负责知识有效性和权限，不负责批准 AI 生成的工程成果。
- 项目经理可发起组织已发布的工作流并选用已发布算法，但不能修改已发布模板或算法逻辑。
- 校核人员原则上不直接修改送审模型，只能提出问题、退回或批准。
- 是否允许设计人审批本人提交由组织策略决定；高风险项目建议强制职责分离。
- 系统管理员默认无权查看业务正文；因运维确需访问时必须临时授权并审计。

## 4. 产品范围与非目标

### 4.1 MVP 范围

- 登录、用户、组织、角色、权限、项目与审计。
- 水利组件目录、低代码组件配置、端口与参数 Schema、版本发布。
- 拖拽式拓扑构建、连接校验、参数配置、基础规则校核。
- 工程模型、版本、方案场景、运行快照、算法资产、基础联合仿真和至少一类专业求解器接入。
- 基础 DAG 工作流、人工审批、失败重试和运行留痕。
- 文档上传、解析、切片、元数据、全文与向量索引、审核与发布。
- 自然语言模型草案、设计文件草案、校核报告草案及强制引用。
- 成果预览、版本、审批、发布、归档和交付包。
- 明渠建模与污水处理作为建议示范场景，最终范围待立项确认。

### 4.2 非目标

- MVP 不覆盖所有水利专业和全部对象，优先验证代表性场景的端到端闭环。
- MVP 不自行研发通用水文、水动力、水质和优化调度求解器，优先接入成熟引擎。
- MVP 不替代 GIS、BIM、CAD 和生产级 SCADA、PLC，仅提供必要关联、数据交换和策略验证能力。
- MVP 不支持未经人工审批的自动设计发布、自动校核签署或生产控制下发。
- MVP 不建设通用算法开发 IDE；自定义算法通过受控包或适配器接入，复杂多求解器联合仿真进入后续版本。
- MVP 不建设通用企业文档管理、即时通信、财务或采购系统。
- MVP 不实现复杂循环工作流、大规模实时协同编辑和跨组织联合审批。
- MVP 不将 AI 回答视为工程依据；没有可靠引用的内容不得进入已验证或正式发布状态。

### 4.3 立项假设

- 主要客户暂按水利设计院、工程咨询单位、水利管理部门和工程业主考虑。
- 部署暂按私有化或专有云优先，是否允许调用外部大模型由数据分级策略决定。
- MVP 建议周期为 4 至 6 个月，至少接入一类专业求解器。
- 典型工程规模、并发量、适用标准、成果模板、电子签章和密级要求均为 TBD。

## 5. 核心领域模型

### 5.1 主对象链

产品以以下对象链组织业务：

`组件模板 -> 组件实例 -> 工程模型 -> 方案场景 -> 运行实例 -> 成果`

| 对象 | 定义 | 关键内容 |
| --- | --- | --- |
| 组件模板 | 可复用的水利对象定义 | 图标、端口、参数 Schema、单位、规则、模型适配器 |
| 组件实例 | 工程模型中的具体对象 | 坐标、实例参数、连接、数据绑定、模板版本 |
| 工程模型 | 组件实例和连接形成的系统 | 拓扑、全局参数、子模型、生命周期、版本 |
| 方案场景 | 同一模型的一组运行条件 | 边界、初始条件、计算时段、工况、调度策略、参数覆盖 |
| 运行实例 | 一次具体计算或工作流执行 | 输入快照、引擎版本、日志、状态、资源消耗 |
| 成果 | 运行或人工提交产生的交付物 | 模型包、数据、图表、设计文件、校核报告、审批记录 |

### 5.2 支撑对象

| 对象 | 作用 |
| --- | --- |
| 项目 | 隔离成员、模型、知识、工作流、运行和成果的数据边界 |
| 连接 | 描述组件端口之间的水力、工艺、数据或控制关系 |
| 数据集 | 保存边界、初始、监测、历史及标定数据及其版本 |
| 算法模型 | 保存水文、水动力、控制或调度算法的输入输出、单位、依赖、运行环境及版本 |
| 规则与规则集 | 保存可执行校核逻辑、适用范围、依据、级别及版本 |
| 知识文档与切片 | 保存原文件、结构化内容、可定位证据和检索索引 |
| 工作流模板与实例 | 定义过程编排，并保存每次执行的节点状态和证据 |
| 审批任务 | 绑定待审对象、版本、处理人、意见、结论与时间 |
| 审计事件 | 记录主体、动作、对象、前后值、时间、来源和追踪标识 |

## 6. 信息架构与核心流程

### 6.1 信息架构

- **管理端：** 登录、模型管理、算法管理、工作流管理、智能体管理、知识管理、水利组件、成果策略、系统管理。
- **用户端：** 登录、项目管理、拖拽工程构建、校核、AI 对话、模型运行、测试报告、评估报告、成果管理。

### 6.2 核心用户流程

1. **组件发布：** 领域专家定义端口和参数，绑定校核规则及求解器，在沙箱验证后审核发布。
2. **拖拽建模：** 工程师创建项目和模型，拖入组件并连接端口，配置参数和数据，实时校核后保存模型版本。
3. **方案计算：** 工程师基于已校核模型创建场景，绑定已发布算法，配置边界、工况和调度策略，执行联合仿真并对比结果。
4. **AI 文件生成：** 用户选择模型、运行成果和模板，输入自然语言要求，检索授权知识并生成带引用草案。
5. **人工复核：** 系统执行结构、规则与引用校验，工程师修改，校核人员复核，审批人员给出结论。
6. **成果交付：** 系统关联模型、运行、规则、知识及审批版本，批准后发布并归档交付包。

### 6.3 标准端到端流程

`创建项目 -> 选择模板 -> 拖拽拓扑 -> 配置参数 -> 编排算法 -> 基础校核 -> 规范校核 -> 创建场景 -> 联合仿真或调度 -> 结果评估 -> 生成文件草案 -> 人工复核 -> 审批发布 -> 成果归档`

## 7. 功能需求

### 7.1 登录与项目管理

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| IAM-01 | P0 | 支持本地账号登录，预留统一身份认证接口 | 登录失败锁定、会话超时和密码策略可配置；鉴权由服务端执行 |
| PRJ-01 | P0 | 创建、查询、归档项目并维护项目成员 | 项目名称或编码唯一；归档项目只读 |
| PRJ-02 | P0 | 按项目隔离模型、知识、工作流、运行和成果 | 无权限用户通过页面、搜索、文件地址或 API 均不能获取数据 |
| PRJ-03 | P1 | 通过项目模板复用组件集、模型模板、规则集和工作流 | 模板更新不能静默覆盖项目已有数据 |

### 7.2 水利对象与组件目录

| 分类 | 典型对象 | 典型端口或输入输出 |
| --- | --- | --- |
| 边界与水源 | 降雨、上游来流、下游水位、取水口、排放口、水源点 | 流量、水位、水质、时间序列 |
| 输配水对象 | 河段、明渠、压力管道、隧洞、涵洞、渡槽、倒虹吸 | 上游与下游水力端口 |
| 蓄水对象 | 水库、湖泊、调蓄池、水池、水箱 | 入流、出流、溢流 |
| 水工建筑物 | 闸门、堰、坝、溢洪道、分水口、汇流口 | 水力端口、控制信号 |
| 动力设备 | 水泵、泵站、阀门、启闭机 | 水力端口、电气与控制端口 |
| 监测控制 | 水位计、流量计、雨量站、水质传感器、控制器、执行器 | 数据信号、控制信号 |
| 水文与水动力 | 产汇流、河网演进、一维或二维水动力、水量平衡 | 数据输入、计算输出 |
| 控制与调度 | PID、逻辑控制、规则调度、优化调度 | 状态输入、控制输出 |
| 污水处理 | 格栅、沉淀池、生化池、曝气池、消毒池、污泥处理 | 水量、水质、工艺物流 |
| 通用对象 | 数据源、公式、转换器、延迟器、子模型、外部适配器 | 数据与信号端口 |

首期组件清单和参数标准须由领域专家组冻结。是否将污水处理纳入“水利专用”一期核心范围为待决策项。

### 7.3 组件可配置

每个组件至少支持基础信息、空间与高程、几何、水力或工艺参数、边界与初始条件、运行控制、数据绑定、仿真设置、校核约束、图形与端口配置。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| CMP-01 | P0 | 低代码定义组件名称、分类、图标、端口和样式 | 端口含类型、方向、介质、数据类型和最大连接数；可在沙箱预览 |
| CMP-02 | P0 | 通过参数 Schema 定义字段 | 支持类型、单位、默认值、范围、精度、必填、枚举、说明、条件显示和联动计算 |
| CMP-03 | P0 | 管理模板默认值、实例值、场景覆盖值和运行时值 | 优先级为运行时值高于场景值，高于实例值，高于模板默认值 |
| CMP-04 | P0 | 统一量纲和单位换算 | 底层保存标准值；展示值、单位、来源、修改人和时间可追溯 |
| CMP-05 | P0 | 支持草稿、测试、审核、发布、停用和版本升级 | 已发布版本不可原地修改；历史模型锁定原版本；升级前展示兼容性影响 |
| CMP-06 | P0 | 绑定校核规则、知识条款和模型适配器 | 发布前验证引用与输入输出映射，验证失败不得发布 |
| CMP-07 | P1 | 导入和导出组件包 | 包含版本、依赖、内容哈希和签名；冲突由用户显式处理 |

### 7.4 拖拽式拓扑构建

画布同时表达水力对象、工艺对象、数据和控制信号。环路是否有效由组件规则和求解器能力判断，不得简单禁止所有环路。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| TOP-01 | P0 | 组件库支持分类、搜索、收藏和拖入 | 拖入后生成全局唯一实例标识 |
| TOP-02 | P0 | 支持移动、缩放、框选、多选、复制、对齐、分组和撤销重做 | 建议保留不少于 50 步；重开后坐标、分组和视图稳定恢复 |
| TOP-03 | P0 | 通过类型化端口连接并显示方向 | 校验端口类型、介质、数据类型、方向和连接数；非法连接即时阻止并解释原因 |
| TOP-04 | P0 | 实时发现悬空端口、孤立节点、边界缺失和标识冲突 | 问题分错误、警告、提示；阻断错误未消除时不能运行或发布 |
| TOP-05 | P0 | 在属性面板编辑组件与连接参数 | 前后端使用同一 Schema；问题可从清单定位并高亮对象 |
| TOP-06 | P1 | 支持子模型折叠、小地图和大型拓扑导航 | 可按编码、类型、错误状态定位；折叠不改变模型语义 |
| TOP-07 | P2 | 支持 GIS 底图、坐标和里程桩号 | 坐标系与高程基准明确；底图不可用不影响拓扑编辑 |
| TOP-08 | P0 | 通过自然语言生成拓扑草案 | 只使用已发布组件；展示新增、修改、删除差异，确认后才写入草稿 |

拓扑业务规则：

- 连接必须满足端口类型、方向、介质、量纲和数据类型兼容性。
- 双向流、回流工艺或环网组件必须由模板显式声明，求解器须支持其语义。
- 控制信号不能直接连接水力端口，必要时通过控制器或转换组件连接。
- 单连接端口达到最大连接数后禁止继续连接。
- 允许多个独立子系统，但每个可运行子系统必须具备完整边界条件。
- 删除组件前展示连接、工作流、运行和成果依赖；已被正式对象引用时只能停用或归档。

### 7.5 模型与场景管理

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| MDL-01 | P0 | 按组织、项目、专业、类型、状态和标签管理模型 | 支持新建、复制、导入、导出、归档及从模板创建 |
| MDL-02 | P0 | 管理模型版本、基线、差异和回滚 | 已提交或已发布模型不可覆盖；回滚生成新版本 |
| MDL-03 | P0 | 管理模型生命周期 | 状态变更必须通过权限和工作流校验 |
| MDL-04 | P0 | 支持方案场景及参数覆盖 | 场景保存边界、初始条件、计算时段、工况和调度策略，不修改基础模型 |
| MDL-05 | P0 | 记录完整依赖与运行快照 | 模型、场景、组件、数据、规则、知识、工作流和求解器版本均可追溯 |
| MDL-06 | P1 | 主模型引用水文、水动力、控制、调度及水质子模型 | 接口具有明确版本；循环依赖在保存前阻止 |
| MDL-07 | P0 | 支持多人编辑冲突控制 | MVP 至少采用单写者编辑锁；后续可升级为乐观锁与显式合并，任何阶段均不得静默覆盖 |

### 7.6 算法、工况与联合仿真

算法既可以是平台内已发布的公式、逻辑、PID 或调度组件，也可以通过适配器接入外部模型。MVP 重点支持算法资产治理、输入输出映射和一个专业求解器与算法或控制模型之间的基础联合仿真，不以通用代码开发环境为目标。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| ALG-01 | P0 | 注册并版本化管理水文、水动力、控制和调度算法资产 | 记录算法类型、来源、输入输出 Schema、单位、依赖、运行环境、许可、负责人和版本；已发布版本不可原地修改 |
| ALG-02 | P0 | 通过组件或工作流编排算法调用关系 | 仅允许连接类型、方向、量纲和采样语义兼容的输入输出；工作流禁止循环，算法图阻断求解器不支持的瞬时代数环，允许显式状态或延迟且求解器支持的反馈环 |
| ALG-03 | P0 | 将算法方案绑定到工程模型和方案场景 | 场景可覆盖边界、初值、计算时段、时间步长、事件和调度策略，但不修改基础模型或算法版本 |
| ALG-04 | P0 | 执行基础联合仿真和调度验证 | 支持至少一个专业求解器与算法或控制模型的时间同步、数据交换、事件传播、超时与失败处理；全部交换数据可追溯 |
| ALG-05 | P0 | 校验并冻结算法运行上下文 | 运行前校验输入完整性、单位、时间基准和版本兼容性；快照锁定算法包、依赖、配置、随机种子及环境哈希 |
| ALG-06 | P1 | 接入自定义算法包 | 在隔离环境执行安全扫描、签名、资源配额和接口契约测试；未通过沙箱验证不得发布 |
| ALG-07 | P1 | 支持多求解器联合仿真 | 明确主时钟、步长协商、数据交换点、收敛条件和失败传播策略；使用跨求解器黄金算例验证结果与复现性 |

### 7.7 模型校核与运行

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| CHK-01 | P0 | 执行拓扑、参数、单位和边界完整性校核 | 每项问题包含级别、对象、参数路径、来源和修复建议，并可定位到画布 |
| CHK-02 | P0 | 按版本化规则集执行规范校核 | 锁定规则版本；结果为通过、不通过、警告、无法校核或不适用 |
| CHK-03 | P0 | 支持问题确认、驳回、豁免和复核 | 记录人员、时间、意见及状态；可比较新增、已解决和遗留问题 |
| CHK-04 | P0 | 判定模型在指定工况下的可运行性及适用等级 | 按经批准的等级体系、版本化规则和仿真指标输出结论、成立条件、等级、依据、未覆盖项和异常；未冻结等级体系或证据不足时标记无法校核，不由 AI 单独裁决 |
| RUN-01 | P0 | 运行仿真、控制或调度模型 | 显示排队、运行、成功、失败、取消、超时状态及进度、日志、取消和重试入口 |
| RUN-02 | P0 | 冻结可复现的运行快照 | 记录输入、组件、规则、求解器、环境、随机种子和内容哈希；相同快照结果在容差内一致 |
| RUN-03 | P0 | 隔离长任务与交互服务资源 | 配置并发、超时、配额和优先级；超限任务排队，不拖垮交互服务 |
| RUN-04 | P1 | 支持多方案结果对比 | 统一指标、时间轴和单位；差异可保存为评估成果 |

### 7.8 工作流编排

MVP 采用有向无环图编排，支持人工任务、规则任务、仿真任务、AI 任务和审批网关。任意循环、复杂补偿及跨组织联合审批不进入 MVP。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| WF-01 | P0 | 图形化配置节点、连线、输入输出和负责人 | 发布前校验缺失输入、类型冲突、无出口节点和非法循环 |
| WF-02 | P0 | 支持串行 DAG、人工审批和条件网关 | 节点定义超时、重试、失败策略、权限和日志级别 |
| WF-03 | P0 | 工作流模板版本化 | 已发布模板不可原地修改；项目实例锁定模板版本 |
| WF-04 | P0 | 实例支持暂停、继续、取消和失败节点重试 | 重试不得重复产生已成功副作用；成功节点输出不可篡改 |
| WF-05 | P0 | 人工审批记录完整证据 | 记录审批人、意见、时间、结论和所审输入版本；退回后生成新版本 |
| WF-06 | P1 | 支持并行、定时、事件触发和通知 | 触发去重；汇聚规则明确；通知失败不改变业务状态 |

节点类型至少包括数据导入与预处理、模型完整性校核、规范校核、模型编译、仿真运行、控制与调度、AI 草案、报告生成、人工复核、审批、条件判断、通知及外部 API。

### 7.9 知识库与知识治理

标准入库链路：`上传 -> 安全扫描 -> 解析或 OCR -> 结构恢复 -> 切片 -> 元数据补录 -> 质量检查 -> 建索引 -> 人工审核 -> 发布`。

只有已发布且在有效期内的知识默认进入生产检索。

MVP 默认面向可直接提取文本的电子文档，具体支持格式为 TBD。KB-02 与 KB-03 的 P0 范围是保留原生电子表格和公式的已有结构并避免无语义切断，不包含扫描件、跨页复杂表格或公式识别；后者与 OCR 按 P1 规划。若立项确认扫描件为一期刚需，应同步调整 KB-07 优先级、解析器选型和验收样本。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| KB-01 | P0 | 批量上传、断点续传和重复检测 | 按文件哈希识别重复；失败重试不生成重复记录 |
| KB-02 | P0 | 解析正文、标题、页码、列表、表格、图片说明和附件关系 | 任一切片可回溯至原文件、页码、章节和表格位置 |
| KB-03 | P0 | 按标题层级、条款和语义切片 | 强制条文、公式、单位和表格不得无语义截断；保留父子及相邻关系 |
| KB-04 | P0 | 管理业务、权限、版本、效力和技术元数据 | 缺少必填元数据不得发布；水利词表由领域专家治理 |
| KB-05 | P0 | 建立向量、关键词和元数据索引 | 索引任务可观察、可重建；记录解析器和嵌入模型版本 |
| KB-06 | P0 | 支持审核、发布、撤回、废止和版本替代 | 新版本不覆盖旧版本；历史任务保留知识快照标识，访问快照正文时仍执行当前权限和强制下架策略 |
| KB-07 | P1 | 支持扫描件 OCR 与低置信度复核 | 低置信度页面进入人工队列，不直接进入生产索引 |
| KB-08 | P1 | 规则冲突、失效和更新提醒 | 冲突不自动裁决；展示效力、适用条件和来源 |

知识元数据至少包括：

- 唯一标识：文档 ID、版本 ID、切片 ID、内容哈希。
- 业务范围：租户、组织、项目、流域、区域、专业、水利对象类型。
- 文件属性：标题、文号、文档类型、发布单位、语言、原文件地址。
- 版本效力：版本号、发布日期、生效日、失效日、现行或废止、替代关系。
- 知识属性：章节、条款、关键词、规则类型、适用阶段、组件类型。
- 权限属性：密级、可见组织、可见项目、可见角色、知识负责人。
- 技术追踪：页码、版面位置、解析器、OCR 置信度、嵌入模型、索引时间。
- 治理信息：创建、审核、发布人员及意见、时间和废止原因。

### 7.10 检索增强生成

检索链路为：意图识别、服务端权限过滤、查询改写与水利同义词扩展、向量和关键词混合检索、元数据过滤、基础重排序、规则时效检查、上下文组装、生成及引用校验。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| RAG-01 | P0 | 在服务端完成权限预过滤和混合检索 | 关键词、向量和元数据检索只处理当前用户有权访问的内容；不得先召回越权内容再在界面隐藏 |
| RAG-02 | P0 | 按项目、专业、对象、地域、版本和效力过滤并执行基础重排序 | 默认优先现行有效且适用范围匹配的知识；过滤与排序参数写入生成记录 |
| RAG-03 | P0 | 冻结每次生成使用的知识快照 | 保存文档版本、切片、检索分数和排序结果；快照冻结内容版本但不冻结访问授权 |
| RAG-04 | P0 | 对生成结论执行引用完整性和一致性校验 | 关键结论缺少可定位引用、引用失效或证据不支持结论时，不得标记为已验证或进入发布 |

- 默认优先现行有效、地域匹配、项目匹配和模型类型匹配的知识。
- 同一问题存在冲突规则时，展示冲突、效力和适用条件，由授权人员处理。
- 每次生成固定知识快照，保存文档版本、切片、检索分数和重排结果。
- 每个关键结论必须展示文件标题、文号、版本、页码或条款、原文片段、效力状态和跳转入口。
- 引用缺失、定位失败或与结论不一致时，对应内容不能标记为已验证，也不能进入正式发布状态。

### 7.11 自然语言建模与 AI 文件生成

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| AI-01 | P0 | 创建带项目、模型、规则和知识上下文的会话 | 明确显示生效上下文；用户可限定文档、规则集或模型版本 |
| AI-02 | P0 | 识别工程意图并追问关键缺失条件 | 不臆测影响安全、合规或计算的参数；假设和待确认项可见 |
| AI-03 | P0 | 生成符合 Schema 的设计中间表示 | 包含组件、端口、连线、参数、单位、来源、置信度、假设和引用 |
| AI-04 | P0 | 调用组件目录、规则引擎、模型校验器和仿真服务 | 只调用白名单工具；输入输出均记录；数值结论来自确定性服务 |
| AI-05 | P0 | 展示模型差异并逐项采纳 | 新增、修改、删除和风险项可见；接受、驳回和编辑操作均留痕 |
| AI-06 | P0 | 生成设计文件和校核报告草案 | 关联模型、规则、知识、运行和模板版本；关键结论引用完整 |
| AI-07 | P0 | 强制人工复核与审批 | AI 初稿、人工修改、审批意见和最终版本完整保存 |
| AI-08 | P0 | AI 生成记录可复现 | 保存模型供应商、模型版本、提示模板、参数、检索片段、工具结果和内容哈希 |
| AI-09 | P1 | 多模型路由、配额、缓存和成本管理 | 按任务和密级选择模型；超额时降级或停止并通知 |
| AI-10 | P2 | 多方案生成、参数敏感性和优化建议 | 以专业算法及人工审批为约束，禁止只凭语言模型闭环控制 |

AI 可调用工具及权限：

| 工具 | 用途 | 权限约束 |
| --- | --- | --- |
| 知识检索 | 按用户、项目、密级、效力和版本检索可定位证据 | 只读 |
| 组件目录 | 查询已发布组件、端口、参数 Schema 和兼容版本 | 只读 |
| 规则与校验 | 执行拓扑、单位、参数及规范规则 | 只读或创建校核任务 |
| 模型草案 | 新建草稿和提交经用户确认的差异 | 只允许写草稿 |
| 仿真任务 | 提交异步运行并查询状态与结果 | 受权限、配额和资源策略约束 |
| 成果草案 | 按模板创建待审文件或成果包 | 不得直接发布 |
| 工作流 | 发起设计复核、校核或成果审批 | 只能执行已发布模板 |

AI 必须遵循以下门禁：

1. 输出默认状态为“草案”。
2. 关键结论必须有可定位、有效且授权可见的引用。
3. 拓扑、Schema、单位、参数和强制规则必须通过确定性校验。
4. 模型变更以差异形式展示，由用户逐项或整体确认后才写入草稿。
5. 草案必须经过具备权限的人员复核与审批，才能形成正式成果。
6. AI 不能发布成果、签署校核结论或向生产控制系统下发指令。

### 7.12 成果管理

成果包括模型包、组件清单、拓扑图、参数清单、运行数据、时序与空间结果、图表、设计说明书、计算书、校核报告、测试报告、评估报告及审批记录。

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| ART-01 | P0 | 统一管理模型、运行、文档、展示和审批成果 | 自动关联项目、模型、场景、运行、工作流、责任人和版本 |
| ART-02 | P0 | 支持在线预览、下载、标签、检索和权限控制 | 下载及授权变更写入审计；文件保存内容哈希 |
| ART-03 | P0 | 管理草稿、待审核、退回、已批准、已发布和已归档状态 | 已发布成果不可覆盖；修订生成新版本并保留关系 |
| ART-04 | P0 | 一键打包可审计交付物 | 包含模型、输入数据、运行日志、结果、报告、引用清单和审批记录 |
| ART-05 | P1 | 支持版本和结构化指标对比 | 展示元数据、指标和文件哈希差异；复杂二进制差异可交给专业工具 |
| ART-06 | P1 | 配置成果模板和保留策略 | 按项目或类型配置模板、归档期限、删除审批和恢复策略 |
| ART-07 | P0 | 生成可追溯的设计验证报告 | 报告正文包含模型与算法版本、工况、输入快照、规则与知识版本、可运行性、等级结论、关键指标、异常和引用；审批后生成关联报告内容哈希的不可变签署清单或签章封套，不回写已审批正文 |

### 7.13 系统管理、权限与审计

| 编号 | 优先级 | 需求 | 验收要点 |
| --- | --- | --- | --- |
| SYS-01 | P0 | 用户、组织、角色、菜单和权限管理 | 页面、按钮、API、文件与数据权限结论一致；默认拒绝 |
| SYS-02 | P0 | 审计日志查询和导出 | 可按用户、模块、动作、对象和时间检索；普通管理员不能修改日志 |
| SYS-03 | P0 | 高风险操作二次确认和职责分离 | 记录操作者、对象、前后值、时间和来源 |
| SYS-04 | P1 | 服务健康、队列、存储、模型调用和告警 | 展示错误率、时延、队列积压、容量、求解器状态和 AI 成本 |
| SYS-05 | P1 | 系统字典、参数和通知渠道 | 配置变更有版本与审计；影响项目行为时展示作用范围 |

## 8. 核心状态模型与业务规则

### 8.1 状态模型

| 对象 | 状态及主要流转 |
| --- | --- |
| 项目 | 可编辑 -> 已归档；归档后只读，恢复编辑须具备项目管理权限并记录审计 |
| 组件模板 | 草稿 -> 测试中 -> 待审核 -> 已发布 -> 已停用；审核退回后回到草稿 |
| 工程模型 | 草稿 -> 待校核 -> 校核退回或校核通过 -> 已发布 -> 已归档 |
| 算法模型 | 草稿 -> 沙箱测试 -> 待审核 -> 已发布 -> 已停用；修改已发布算法必须生成新版本 |
| 校核问题 | 待处理 -> 已确认 -> 已修复或已豁免 -> 已复核；驳回后回到待处理 |
| 运行实例 | 排队 -> 运行中 -> 成功、失败、已取消或超时；失败和超时可创建新重试实例 |
| 工作流模板 | 草稿 -> 待审核 -> 已发布 -> 已停用 |
| 工作流实例 | 待启动 -> 运行中 -> 已暂停、已完成、已失败或已取消 |
| 知识文档 | 已上传 -> 解析中 -> 待复核 -> 待审核 -> 已发布 -> 已撤回或已废止 |
| AI 产物 | 生成中 -> 草案 -> 差异确认 -> 确定性校验 -> 待复核 -> 退回或已通过；通过后仍需进入成果审批 |
| 成果 | 草稿 -> 待审核 -> 退回或已批准 -> 已发布 -> 已归档 |

### 8.2 数据与版本规则

- 所有项目、组件、模型、算法、场景、规则、知识、工作流、运行和成果使用全局唯一标识。
- 已发布对象不可原地修改；变更形成新版本，旧版本可查询和追溯。
- 文件、结构化模型、规则包和成果包保存内容哈希。
- 正式运行快照记录模型、算法、场景、组件、数据、规则、知识、提示模板、求解器和环境版本。
- 参数记录值、单位、来源、修改人、修改时间和前后差异。
- 被引用或已发布对象只能停用或归档；仅未被引用的草稿可物理删除。
- 工作流开始时冻结输入；修改输入必须取消当前实例并重新发起。
- 成果必须可反向追溯至运行实例及全部输入版本。
- 知识快照冻结内容与版本，不冻结访问授权；查看历史正文时重新执行当前 ACL，涉密升级、版权撤回或安全隔离可强制阻断正文，仅保留审计元数据。
- 登录、查看、下载、生成、修改、审批、发布、废止和权限变更均形成审计事件。

### 8.3 校核与例外规则

- 阻断错误未消除时，模型不得运行、送审或发布。
- 规则结果包括通过、不通过、警告、无法校核和不适用。
- 豁免必须填写原因、依据、处理人、审批人和有效范围，不能删除原问题。
- 规则版本升级不追溯改变历史结论；重新校核产生新记录。
- 相同快照在同一求解器版本和容差设置下应得到一致结果。

## 9. AI 质量、评测与异常处理

### 9.1 AI 上线门槛

| 维度 | 指标 | 建议门槛 | 验证方法 |
| --- | --- | --- | --- |
| 解析 | 受支持文档解析成功率 | 不少于 98% | 按格式和扫描质量分层统计 |
| 原文定位 | 切片到页码、章节或表格定位完整率 | 100% | 随机抽检与自动链接校验 |
| 检索 | Recall@10 与 nDCG@10 | 不少于 90% 与 0.80 | 不少于 200 个领域问答 |
| 引用 | 引用存在率与结论一致率 | 100% 与不少于 98% | 专家双人评分，争议仲裁 |
| 忠实度 | 关键结论有证据且不歪曲原文 | 不少于 95% | 覆盖过期、冲突和不适用规则 |
| 结构化设计 | Schema 合法率和提交前强校验 | 100% | 不少于 50 个设计任务 |
| 校核报告 | 报告与规则及仿真结果一致率 | 不少于 98% | 不少于 50 个校核任务 |
| 权限安全 | 跨组织、跨项目或越密级泄漏 | 零容忍 | 越权与提示注入专项测试 |
| 人工效果 | 首次通过或仅轻微修改比例 | 不少于 70% | 按专业与任务难度分层 |
| 性能 | 检索与完整设计草案 P95 | 2 秒内与 60 秒内 | 不含外部仿真时长 |

上线阻断项：引用伪造、严重规则漏检超标、跨项目或越密级泄漏、未经确认写入正式模型、绕过确定性校验，以及提示注入导致模型服从文档指令、隐藏或伪造引用、泄漏上下文或越权调用任何工具。上述提示注入攻击成功率和敏感上下文泄漏率均须为 0。

### 9.2 降级与异常处理

| 场景 | 系统行为 |
| --- | --- |
| 向量服务不可用 | 降级为关键词和元数据检索，并明确提示语义检索不可用 |
| 大模型超时或不可用 | 保留会话和任务上下文，允许重试，并提供模板化人工流程 |
| 解析置信度过低 | 文档进入待复核，不写入生产索引 |
| 证据不足 | 追问或标记证据不足，不输出确定性结论 |
| 规则冲突 | 展示条款、效力和适用条件，交由授权人员处理 |
| 引用定位失败 | 对应结论不得进入已验证或发布状态 |
| 模型校验失败 | 保留草案并展示错误，不允许提交正式模型 |
| 达到配额 | 停止新增生成，保留已完成结果并通知用户和管理员 |

## 10. 技术规格

### 10.1 逻辑架构

建议采用模块化服务架构。具体前后端框架、关系数据库、对象存储、全文检索、向量库、工作流引擎、消息队列、大模型供应商和部署平台均为 **TBD**，由架构评审依据私有化能力、许可、运维成本、安全要求及现有基础设施确定。

| 层级 | 组成 | 职责 |
| --- | --- | --- |
| 体验层 | 管理端、用户端、图形设计器、AI 对话、审核与成果页面 | 项目上下文与可视化交互 |
| 接入层 | API 网关、身份认证、文件上传、实时通信 | 鉴权、限流、幂等、路由、追踪 |
| 领域服务层 | 项目、组件、模型、场景、校核、工作流、成果、系统管理 | 业务规则、版本、状态机、权限 |
| AI 服务层 | 知识治理、混合检索、AI 编排、模型网关、评测护栏 | RAG、工具调用、引用、校验、成本 |
| 计算层 | 模型适配器、任务队列、规则引擎、仿真、控制、调度 | 异步运行、资源隔离、运行快照 |
| 数据层 | 关系数据库、对象存储、全文索引、向量索引、缓存 | 元数据、文件、证据、索引、临时状态 |
| 平台层 | 配置、密钥、审计、日志、指标、链路、告警、备份 | 安全、可观测、容灾、运维 |

### 10.2 关键数据流

1. **组件发布流：** 管理端提交定义，服务端校验 Schema、端口、规则和适配器，沙箱测试后发布不可变版本。
2. **模型设计流：** 用户在画布编辑草稿，服务端增量校验并保存；送审时冻结模型和依赖快照。
3. **知识入库流：** 文件经安全扫描和解析，生成结构化内容及切片，经审核后写入全文与向量索引。
4. **AI 生成流：** 服务端先鉴权和检索，再调用目录、规则与校验服务，生成差异草案供人工确认。
5. **模型运行流：** 工作流冻结输入，计算服务调度求解器，结果和日志写入成果服务并触发审批。
6. **成果发布流：** 成果服务校验来源链、审批和文件哈希，发布不可变版本并按策略归档。

### 10.3 集成点

| 集成点 | 建议方式 | 用途 | 阶段 |
| --- | --- | --- | --- |
| 身份体系 | OIDC、SAML、LDAP 或现有统一认证 | 用户、组织、角色和单点登录 | TBD |
| 专业求解器 | 版本化适配器接口与异步任务协议 | 仿真、控制、调度、水质计算 | MVP 至少一类 |
| 数据源 | 文件、数据库、时序服务或 API | 边界、初始、监测和历史数据 | 按场景确认 |
| GIS、BIM、CAD | 开放格式或服务接口 | 空间叠加、模型关联、成果跳转 | P2；若立项前置则走范围变更评审 |
| 文档与 OCR | 解析器、OCR、表格和公式识别 | 知识入库与原文定位 | 产品选型 TBD |
| 大模型服务 | 统一模型网关 | 推理、路由、配额、审计和回退 | 私有或经批准的服务 |
| 消息与通知 | 邮件、企业消息或事件总线 | 任务、审批、失败和到期提醒 | P1 |
| 电子签章与档案 | 成果发布和归档接口 | 签署、法定归档和长期保存 | TBD |

### 10.4 API 原则

- API 按组件、模型、算法、运行、知识、AI、工作流、成果和系统管理分组。
- 请求统一携带用户、租户、项目和追踪上下文。
- 创建类接口支持幂等键；长任务返回任务 ID。
- 错误响应包含稳定错误码、可修复建议和追踪 ID。
- 破坏性接口变更必须版本升级并设置迁移期。

## 11. 安全、隐私与审计

| 控制域 | 要求 |
| --- | --- |
| 身份与授权 | 采用 RBAC 与 ABAC 组合；角色控制动作，组织、项目、密级和状态控制数据范围 |
| 最小权限 | 默认拒绝；页面与 API 同源鉴权；高风险操作二次确认；支持职责分离 |
| 数据隔离 | 关系库、对象存储、全文索引、向量检索和 AI 上下文执行同一权限过滤 |
| 加密与密钥 | 传输和存储加密；密钥进入专用管理系统，不写入代码、日志或提示词 |
| 模型数据政策 | 默认禁止模型供应商使用业务数据训练；外部调用受密级和数据出境策略约束 |
| 文件安全 | 恶意文件扫描、格式伪装检测、压缩炸弹限制、宏和脚本隔离；解析过程沙箱化 |
| 提示注入 | 文档作为不可信证据隔离；工具白名单；高风险调用需要确定性授权和参数校验 |
| 审计与留存 | 关键操作日志不可篡改；提示词、原文和个人信息按最小必要原则保存 |
| 合规 | 等保等级、数据分级、日志留存、涉密边界、电子签章和归档要求均为 TBD |

审计事件至少记录：操作者、组织、项目、会话、动作、对象及版本、前后值摘要、时间、来源地址、结果、失败原因和追踪 ID。

## 12. 非功能需求

| 编号 | 领域 | 要求 | 说明 |
| --- | --- | --- | --- |
| NFR-01 | 画布性能 | 500 节点、1000 连线下保持 30 FPS；常用编辑动作 P95 不高于 100 毫秒 | 建议基线，待典型工程压测 |
| NFR-02 | 业务接口 | 普通列表和详情 P95 不高于 1 秒；模型保存 P95 不高于 2 秒 | 不含大文件和长任务 |
| NFR-03 | 知识与 AI | 检索 P95 不高于 2 秒；首次可见响应 P95 不高于 5 秒；完整草案 P95 不高于 60 秒 | 不含外部仿真 |
| NFR-04 | 容量 | 单文件上限建议 500 MB；单批建议不超过 100 个文件 | 最终值按资源和场景确认 |
| NFR-05 | 可用性 | MVP 月可用性不低于 99.5%；V1.1 目标 99.9% | 维护窗口口径 TBD |
| NFR-06 | 容灾 | 核心元数据建议 RPO 不高于 15 分钟，RTO 不高于 4 小时 | 成果文件指标按成本确认 |
| NFR-07 | 兼容性 | 支持 Chrome 和 Edge 最近两个大版本；移动端只读 | 建模以桌面端为主 |
| NFR-08 | 可扩展性 | 组件、求解器、解析器、模型供应商和存储均通过适配器接入 | 数据支持开放格式导出 |
| NFR-09 | 可观测性 | 覆盖 API、队列、模型调用、解析、检索、求解器和工作流 | 日志、指标、链路和业务审计分层 |
| NFR-10 | 可维护性 | 配置、规则、模板和适配器均版本化；数据变更可回滚 | 发布前执行兼容性检查 |
| NFR-11 | 可访问性 | 表单、列表、对话框和管理页面按 WCAG 2.1 AA 设计 | 专业画布替代路径在原型评审确认 |
| NFR-12 | 响应式边界 | 1366 × 768 及以上桌面视口主要操作可见 | 低分辨率可折叠面板，不得遮挡画布 |

## 13. 交互与错误处理原则

- 始终显示当前项目、模型版本、方案场景和编辑状态，跨模块跳转不丢失上下文。
- 错误在产生位置即时提示，并说明原因、影响和修复路径。
- 错误、警告和提示使用文字与图标共同区分，颜色不得作为唯一状态表达。
- 删除、覆盖、发布、回滚和取消运行等高风险动作展示影响范围，并提供二次确认或恢复机制。
- 解析、索引、仿真和报告生成异步执行，显示排队、进度、取消和失败重试。
- 画布支持键盘操作、缩放、对象搜索和问题定位。

## 14. 验收策略

### 14.1 测试层级

| 测试层 | 范围 | 通过条件 |
| --- | --- | --- |
| 单元与组件测试 | Schema、端口、单位、状态机、算法接口、时间同步、规则表达式和版本逻辑 | 核心领域逻辑覆盖率门槛由技术负责人冻结 |
| 集成测试 | 身份、对象存储、搜索、向量库、工作流、算法适配器、求解器、AI 网关 | 覆盖超时、重试、幂等、降级和权限 |
| 黄金算例 | 领域专家为已冻结的首期示范场景提供参考模型及容差 | 求解器升级必须回归全部算例 |
| AI 离线评测 | 知识问答、设计、校核、过期冲突规则和提示注入 | 达到第 9.1 节门槛后方可灰度 |
| 性能压测 | 大拓扑、大文件、检索、并发生成、长任务队列和成果打包 | 达到第 12 节基线或经审批调整后的指标 |
| 安全测试 | 越权、文件攻击、敏感信息、提示注入、工具滥用和日志泄漏 | 跨项目或越密级泄漏为上线阻断；提示注入指令服从、引用绕过、上下文泄漏和越权工具调用成功率均为 0 |
| 用户验收 | 3 至 5 个标准案例完成建模、校核、运行、审批和归档 | P0 核心验收项通过率 100% |
| 恢复演练 | 数据库、对象存储、索引、配置和任务恢复 | 满足冻结后的 RPO、RTO 并形成操作手册 |

### 14.2 端到端验收场景

1. **明渠设计闭环：** 从组件拖拽、断面及边界配置到运行、校核、报告、审批和归档全流程完成。
2. **污水处理流程闭环：** 构建工艺拓扑，校验介质与端口，配置工况并生成评估成果；是否进入 MVP 待范围确认。
3. **算法联合仿真：** 将已发布控制或调度算法绑定工程模型，在指定工况下完成时间同步、数据交换、运行复现和异常处理验证。
4. **可运行性与等级：** 使用经领域专家确认的正反黄金算例，验证指定工况下的可运行性、等级结论、成立条件、无法判级状态和证据链。
5. **组件与算法升级：** 发布新版本，历史模型保持可运行，新项目可选新版本，升级影响可预览。
6. **知识更新：** 新规范替代旧版本；历史 AI 任务保留原知识快照，但查看正文仍受当前权限和强制下架策略控制；新任务默认使用现行版本。
7. **AI 草案：** 自然语言生成模型或文件草案，展示引用、假设和差异；未确认不能写入模型，未审批不能发布成果。
8. **权限隔离：** 未授权用户无法通过页面、搜索、直接链接、API 或 AI 问答获取其他项目数据。
9. **运行复现：** 依据已保存快照重跑，核心结果在领域专家确定的容差内一致。
10. **失败恢复：** 求解器超时、向量库不可用或工作流节点失败时，按降级与重试规则处理且不产生重复副作用。

### 14.3 发布门禁

- P0 需求验收通过率 100%，严重缺陷清零。
- 黄金算例、权限隔离、引用真实性、状态机和版本链测试全部通过。
- AI 达到引用、忠实度、结构合法率及安全门槛。
- 运维、备份恢复、应急停用和审计导出手册齐备。
- 领域、产品、安全、技术和试点用户共同签署上线意见。

## 15. 路线图

| 阶段 | 建议周期 | 主要范围 | 退出条件 |
| --- | --- | --- | --- |
| MVP | 4 至 6 个月 | 用户与项目、RBAC、核心组件、拓扑与校验、模型与算法版本、场景与基础联合仿真、文档入库与检索、AI 设计和校核草案、一类求解器、基础 DAG、成果归档、审计 | 3 至 5 个标准案例闭环；严重缺陷清零；P0 验收 100%；试点用户可独立完成标准任务 |
| V1.1 | MVP 后 3 至 4 个月 | 扩展组件、组件兼容升级、自定义算法包、多求解器联合仿真、OCR 与复杂表格公式、领域重排序优化、并行与定时工作流、成果对比、SSO、高可用与告警 | 文档、RAG 与 AI 达到第 9.1 节对应指标；组件与自定义算法通过 Schema、兼容性、签名、安全扫描和接口契约测试；多求解器通过 ALG-07 黄金算例、复现性和失败传播测试；月可用性 99.9%；至少两个真实项目连续使用一个完整周期 |
| V2.0 | V1.1 后 6 至 9 个月 | 多方案生成与优化、敏感性分析、自动标定、跨项目知识治理、组件生态、GIS、BIM、实时数据与数字孪生能力评估 | 高价值场景证明收益；通过安全与工程正确性专项评审；智能闭环保留人工审批 |

### 15.1 MVP 里程碑

| 阶段 | 时间 | 主要交付 | 门禁 |
| --- | --- | --- | --- |
| 阶段一 | 第 1 月 | 需求冻结、领域对象、组件 Schema、端口语义、单位体系、架构与安全方案 | 原型和接口契约通过评审 |
| 阶段二 | 第 2 至 3 月 | 项目、组件、拓扑、模型版本、基础校核、知识上传与解析 | 明渠标准模型可保存、校核和复用 |
| 阶段三 | 第 3 至 4 月 | 算法管理、求解器接入、基础联合仿真、工作流、成果管理、混合检索、AI 设计草案 | 首个端到端案例跑通 |
| 阶段四 | 第 5 月 | 已确认示范场景的扩展案例、AI 校核报告、权限审计、性能与安全测试 | 试点用户完成 UAT |
| 阶段五 | 第 6 月 | 缺陷收敛、数据迁移、培训、运维手册、灰度上线 | 发布门禁全部通过 |

## 16. 关键依赖

| 依赖 | 必须产出 | 责任建议 | 最晚就绪 |
| --- | --- | --- | --- |
| 水利对象体系 | 首期组件清单、端口语义、连接规则、单位体系 | 领域专家组 | 原型冻结前 |
| 组件 Schema | 参数规范、版本与兼容策略 | 产品、架构、领域 | 开发启动前 |
| 工程规则 | 可执行规则、证据来源、严重级别 | 校核专家组 | 校核联调前 |
| 算法资产 | 首期算法清单、输入输出 Schema、单位、运行环境、许可和参考结果 | 算法与控制团队 | 联合仿真联调前 |
| 专业求解器 | 接口、许可、参考算例、容差、运行环境 | 算法与仿真团队 | MVP 中期 |
| 领域语料 | 可合法使用的规范、历史设计、脱敏要求 | 知识管理、法务 | 知识联调前 |
| AI 评测集 | 问题、标准答案、引用、风险级别和评分规则 | 领域与 AI 团队 | AI 内测前 |
| 身份与基础设施 | 组织、身份接口、RBAC、存储、索引、队列和模型服务 | IT、安全、运维 | 集成测试前 |
| 成果标准 | 模板、命名、审批、签章、保留和归档规则 | 项目与档案管理 | UAT 前 |
| 合规要求 | 等保、密级、数据出境、日志和模型使用政策 | 安全、法务 | 架构评审前 |

## 17. 风险与应对

| 风险 | 概率/影响 | 预警信号 | 应对措施 | 责任建议 |
| --- | --- | --- | --- | --- |
| 对象和端口标准不统一 | 高/高 | 同一对象多套定义，原型反复返工 | 冻结 MVP 核心对象，成立领域治理组，执行变更评审 | 产品与领域负责人 |
| 求解器结果偏离参考案例 | 中/极高 | 核心指标误差超容差 | 黄金算例、双重核验、适配器认证、版本锁定 | 算法负责人 |
| AI 虚构参数或依据 | 高/高 | 无来源结论、校验失败 | 强制引用、结构化输出、确定性校验、人工审批 | AI 负责人 |
| 严重校核问题漏检 | 中/极高 | 基准集召回下降或线上漏检 | 高风险规则确定性实现、专家签署、持续回归 | 校核负责人 |
| 表格与公式解析失真 | 高/中 | 条款号缺失、引用定位失败 | 质量抽检、失败隔离、专用解析链路、人工修订 | 知识平台主管 |
| 跨项目或越密级泄漏 | 中/极高 | 越权检索、日志出现敏感内容 | 检索前鉴权、结果二次校验、加密、脱敏和红队测试 | 安全负责人 |
| 文档提示注入 | 中/高 | 模型遵从证据中的恶意指令 | 证据隔离、工具白名单、参数校验、攻击样本回归 | AI 与安全负责人 |
| 版本链断裂 | 中/高 | 成果无法定位输入和环境 | 不可变快照、内容哈希、依赖锁定、发布门禁 | 架构负责人 |
| 大型拓扑编辑卡顿 | 中/中 | 节点增长后帧率下降或保存变慢 | 增量渲染、视区裁剪、后台校验、基准压测 | 前端负责人 |
| 长任务耗尽资源 | 高/高 | 队列积压、交互接口变慢 | 资源隔离、配额、优先级、超时、检查点和告警 | 平台负责人 |
| 工作流重试重复副作用 | 中/高 | 重复运行、报告或审批 | 幂等键、节点状态机、补偿机制和故障注入测试 | 工作流负责人 |
| 组件升级破坏历史模型 | 中/高 | 旧模型校验或运行失败 | 版本锁定、兼容矩阵、升级预览和迁移工具 | 组件负责人 |
| 算法或联合仿真结果不可复现 | 中/极高 | 相同快照输出超出容差或时间同步漂移 | 锁定算法、依赖与运行环境，建立联合仿真黄金算例和时间同步测试 | 算法与仿真负责人 |
| 项目范围持续膨胀 | 高/高 | MVP 场景与组件持续增加 | 固定首期范围、变更委员会、以退出条件控范围 | 产品负责人 |
| 用户继续依赖线下工具 | 中/中 | 活跃率低、成果事后补录 | 真实试点共创、模板、培训和用户漏斗分析 | 产品与实施负责人 |

## 18. 发布与运营机制

- 采用内部验证、受控试点、扩大试点和正式上线四个阶段，每阶段使用同一套黄金算例和安全回归集。
- 灰度期间保留人工流程作为回退；知识、AI 或求解器异常不影响用户查看既有模型与成果。
- 每周审查缺陷、校验失败、AI 采纳、引用问题和队列资源；每月审查成功指标与模型成本。
- 组件、规则、解析器、提示模板、嵌入模型和大模型升级均须版本化评测，禁止直接替换生产版本。
- 发现严重错误设计、越权泄漏或不可复现成果时，立即停用相关 AI 或适配器能力并保留审计证据。

## 19. 待决策项

| 编号 | 决策 | 影响 | 最晚确认 |
| --- | --- | --- | --- |
| D-01 | 首期专业与示范场景是否包含明渠、污水处理，以及是否新增防洪、灌溉、水库调度或供水 | 组件目录、黄金算例和团队配置 | 立项会 |
| D-02 | 需要接入哪些仿真、控制和调度求解器 | 适配器 API、许可和计算资源 | 架构评审前 |
| D-03 | 典型最大组件数、连线数、场景数和并发运行数 | 性能基线和容量设计 | 原型冻结前 |
| D-04 | GIS、BIM、实时监测和二维或三维能力是否进入一期 | 范围与集成复杂度 | 范围冻结前 |
| D-05 | 适用国家、行业、地方和企业标准及其优先级 | 知识治理和规则冲突策略 | 知识联调前 |
| D-06 | 私有化、专有云或混合部署，以及允许使用的大模型范围 | 安全架构、成本和供应商选择 | 架构评审前 |
| D-07 | 等保等级、密级、数据出境、日志和保留要求 | 安全建设与验收清单 | 架构评审前 |
| D-08 | 成果模板、电子签章、法定归档和审签职责 | 成果状态机和外部接口 | UAT 设计前 |
| D-09 | 预算、团队规模和 MVP 最晚上线时间 | 路线图、范围和质量投入 | 立项会 |
| D-10 | 前后端框架、数据库、对象存储、全文与向量检索、队列、工作流引擎和模型供应商 | 技术架构与运维方案 | 架构评审前 |
| D-11 | MVP 支持的文档格式、扫描件比例、单文件复杂度和 OCR 精度要求 | 解析器选型、容量、知识质量和人工复核成本 | 知识方案评审前 |
| D-12 | “可运行性”和“适用等级”的分类体系、枚举、阈值、规则来源、批准责任人及正反黄金算例 | 决定 CHK-04 的规则集、验证报告模板和客观验收口径 | 校核方案评审前 |

## 20. 需求追踪矩阵

| 产品目标 | 需求编号 | 主要用户故事 | 验证方式 |
| --- | --- | --- | --- |
| 身份与项目隔离 | IAM-01、PRJ-01 至 PRJ-03 | 管理员维护项目成员，成员仅访问授权项目 | 登录策略、服务端鉴权、归档只读和越权测试 |
| 拖拽拓扑构建 | TOP-01 至 TOP-08 | 设计工程师构建并校验水利拓扑 | 画布功能、集成、性能与 UAT |
| 组件可配置 | CMP-01 至 CMP-07 | 领域专家配置并发布组件 | Schema、端口、版本、兼容和单位测试 |
| 模型管理 | MDL-01 至 MDL-07 | 工程师管理模型、版本和场景 | 版本、差异、快照和权限测试 |
| 算法与联合仿真 | ALG-01 至 ALG-07 | 算法工程师编排算法并与工程模型联合验证 | 接口契约、单位、时间同步、沙箱和复现测试 |
| 模型校核与运行 | CHK-01 至 CHK-04、RUN-01 至 RUN-04 | 校核人员和仿真人员执行校核、等级判定与计算 | 黄金算例、规则回归和资源测试 |
| 工作流 | WF-01 至 WF-06 | 项目经理编排并执行标准流程 | DAG、幂等、审批和故障注入 |
| 知识库与 RAG | KB-01 至 KB-08、RAG-01 至 RAG-04 | 知识管理员入库并治理规范，工程师基于授权证据生成草案 | 解析、检索、引用、权限和安全评测 |
| 自然语言 AI | AI-01 至 AI-10 | 工程师生成模型与文件草案 | 离线评测、引用、提示注入、工具权限和 UAT |
| 成果管理 | ART-01 至 ART-07 | 项目成员管理和交付成果 | 验证报告、哈希、版本、打包和归档测试 |
| 系统管理 | SYS-01 至 SYS-05 | 管理员配置权限并审查活动 | RBAC、ABAC、审计和安全测试 |
| 非功能质量 | NFR-01 至 NFR-12 | 全体用户稳定、安全地使用系统 | 性能、容量、可用性、恢复、兼容性和可访问性测试 |

## 附录 A：术语表

| 术语 | 定义 |
| --- | --- |
| MBD | Model-Based Design，基于模型的设计，以可计算、可验证模型驱动设计、仿真、校核和交付 |
| 组件模板 | 定义水利对象端口、参数、规则、样式和模型适配器的可复用标准资产 |
| 组件实例 | 组件模板在某个工程模型中的具体引用和配置 |
| 工程模型 | 由组件实例、连接、全局参数和子模型构成的水利系统表达 |
| 方案场景 | 在同一工程模型上定义的一组边界、初始条件、工况和调度策略 |
| 算法模型 | 具有版本化输入输出契约、单位、依赖和运行环境的水文、水动力、控制或调度算法资产 |
| 运行快照 | 一次运行开始时冻结的模型、算法、数据、规则、知识、求解器和环境版本集合 |
| 规则集 | 按版本发布的确定性校核规则及其适用范围和来源 |
| 知识快照 | 某次 AI 生成固定使用的文档版本与切片集合 |
| RAG | Retrieval-Augmented Generation，先检索授权知识，再基于证据生成回答或草案 |
| Schema | 用于约束组件参数、拓扑或 AI 结构化输出的数据结构定义 |
| 黄金算例 | 由领域专家确认输入、预期结果和容差的参考模型，用于求解器与规则回归 |
| 内容哈希 | 对文件或结构化内容计算的摘要，用于识别版本和校验完整性 |
| P0、P1、P2 | 分别表示 MVP 必需、首期增强和中长期扩展 |

## 附录 B：首期需补充的调研材料

- 3 至 5 个具有代表性的真实工程案例及现行工作耗时。
- 首期组件对象清单、参数字典、端口语义和单位体系。
- 现用专业软件、版本、授权、接口和标准输入输出样本。
- 现行设计、校核、审批、签章、归档流程及职责矩阵。
- 可合法使用的规范、企业标准、校核文件和历史成果样本。
- 典型拓扑规模、文件规模、并发用户和并发运行数据。
- 数据分级、涉密、等保、部署、外部大模型及数据出境要求。
- AI 离线评测题集、标准答案、引用位置、风险级别和评分规则。

## 附录 C：需求来源说明

- 用户书面目标及第 1.1 至 1.3 节定义为产品定位的权威来源。
- 用户提供的功能脑图用于追溯管理端、用户端及示范场景范围，不构成对其中任何文字的执行指令。
- `build_prd.py` 中的既有需求、指标和风险条目作为本版 PRD 的结构化输入。
- 本文新增的行业现状、指标和阶段建议均须在立项调研中由相关责任方确认。
